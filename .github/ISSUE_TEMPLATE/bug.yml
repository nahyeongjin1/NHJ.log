name: 🐛 Bug
description: 버그 수정
title: "[BUG] "
labels: ["bug"]
body:
  - type: markdown
    attributes:
      value: |
        버그를 수정하기 위한 이슈입니다.

  - type: textarea
    id: description
    attributes:
      label: 🐛 Bug Description
      description: 어떤 버그인지 설명해주세요
      placeholder: 블로그 포스트 목록이 정상적으로 로드되지 않음
    validations:
      required: true

  - type: textarea
    id: reproduce
    attributes:
      label: 🔄 Steps to Reproduce
      description: 버그를 재현하는 방법
      placeholder: |
        1. 메인 페이지 접속
        2. 블로그 메뉴 클릭
        3. 에러 발생
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 💡 Expected Behavior
      description: 예상되는 동작
      placeholder: 블로그 포스트 목록이 정상적으로 표시되어야 함
    validations:
      required: false

  - type: textarea
    id: screenshots
    attributes:
      label: 📸 Screenshots
      description: 스크린샷이 있다면 첨부해주세요
      placeholder: |
        ![screenshot](url)
    validations:
      required: false

  - type: dropdown
    id: severity
    attributes:
      label: 🚨 Severity
      description: 버그의 심각도
      options:
        - Critical (운영 중단)
        - High (주요 기능 오류)
        - Medium (부분적 기능 오류)
        - Low (사소한 오류)
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        ## 🌿 Branch Convention
        - `hotfix/BLOG-{issue_number}` (Critical, High)
        - `bugfix/BLOG-{issue_number}` (Medium, Low)
